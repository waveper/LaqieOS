-- Laqie OS

- an monolithic kernel that only target i386 range instruction
- build with LLVM toolchain and NASM as an assembler

-- System requirement??
  - likely should use pentium for performance because I don't bless my code with optimization that much.. but i486 is valid
  - the required memory is about 6 Megabytes
uhh that's it
